# broken-syntax.toml — true TOML syntax error so toml.ParseError fires.
# The unclosed [meta table-header is unrecoverable at the lexer level,
# which is exactly the cascade-1 (parse) trigger FormatLoadError must
# classify with line+col precision.
[meta
version = "1"
locale  = "uk"
